# Core AI & Utilities
# Pinned to a known stable version for Runpod compatibility. Dia requires >= 2.0.
torch==2.3.0
torchaudio==2.3.0
# NOTE: transformers is installed from git in the setup instructions.

# ASR (Speech-to-Text)
faster-whisper==1.0.2
ctranslate2==4.3.1

# LLM (Language Model)
accelerate>=0.30.1
bitsandbytes>=0.43.1

# TTD (Text-to-Dialogue - Dia)
# Core dependencies for Nari-Labs Dia, versions cross-referenced for compatibility.
descript-audio-codec>=1.0.0
soundfile>=0.12.1
librosa>=0.10.0

# Web UI
gradio>=4.25.0

# Other Dependencies
huggingface-hub>=0.23.0
numpy
pydantic
